# Development and CI dependencies only. The Lambda container image installs
# nothing - the handler uses only the standard library plus the boto3 that ships
# in the AWS base image.
pytest==8.3.4
moto[s3]==5.0.28
boto3==1.35.99
cfn-lint==1.22.3
ruff==0.9.2
